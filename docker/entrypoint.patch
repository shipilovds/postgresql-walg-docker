--- a/usr/local/bin/docker-entrypoint.sh	2025-02-17 04:35:21.160225147 +0300
+++ b/usr/local/bin/docker-entrypoint.sh	2025-02-17 04:39:43.737558358 +0300
@@ -305,6 +305,16 @@
 	fi
 
 	if [ "$1" = 'postgres' ] && ! _pg_want_help "$@"; then
+		#============================================================================#
+		#=============================| POSTGRES+WALG |==============================#
+		#============================================================================#
+		source "/var/lib/wal-g-utils/common.sh"
+		s3_connect
+		maintenance
+		if [[ "$WALG_ENABLED" == "true" ]]; then
+			validate_s3_prefix
+		fi
+		#============================================================================#
 		docker_setup_env
 		# setup data directories and permissions (when run as root)
 		docker_create_db_directories
@@ -346,6 +356,11 @@
 
 			EOM
 		fi
+		#============================================================================#
+		#===========================| CONFIG GENERATOR |=============================#
+		#============================================================================#
+		generate_postgres_conf
+		#============================================================================#
 	fi
 
 	exec "$@"
